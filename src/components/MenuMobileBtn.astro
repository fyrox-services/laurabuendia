<button id="menu-mobile-btn">
  <div></div>
  <div></div>
  <div></div>
</button>

<script>
  import { handleMenuMobile } from "@/stores";

  const $btn = document.getElementById("menu-mobile-btn");

  if ($btn !== null) {
    $btn.addEventListener("click", () => {
      $btn.classList.toggle("active");
      handleMenuMobile.set();
    });
  }
</script>

<style>
  button {
    display: flex;
    flex-direction: column;
    justify-content: space-around;
    width: 35px;
    height: 35px;
    background-color: transparent;
    border: none;
    padding: 0.2rem;
  }

  button div {
    width: 100%;
    height: 2px;
    background-color: var(--black);
    transform-origin: right;
    transition:
      transform 0.3s ease-in-out,
      opacity 0.3s ease-in-out;
  }

  button div:nth-child(2) {
    width: 50%;
    align-self: flex-end;
  }

  button.active div:nth-child(1) {
    transform: rotate(-40deg);
  }

  button.active div:nth-child(2) {
    opacity: 0;
  }

  button.active div:nth-child(3) {
    transform: rotate(40deg);
  }

  /* DESKTOP */

  @media (min-width: 1024px) {
    button {
      display: none;
    }
  }
</style>
